<?xml version="1.0" encoding="UTF-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

<LinearLayout   android:orientation="vertical"
                android:gravity="center"
                android:background="#000000"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content">

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textColor="#ffffff"
        android:textSize="14dip"
        android:text=
 "Terminal IDE has a basic system. This is installed into this application's private memory. You DO NOT NEED ROOT access or any other special permissions. \n
 \n
 When the system needs to be installed/upgraded with a newer version, you can choose to update when you are ready, here.\n
 \n
 The ~/system folder will be DELETED and re-written. You should not be storing your work in there.\n
 \n
 The files ~/.vimrc, ~/.vim, ~/.tmux.conf, ~/.bashrc and mc.ini will NOT BE over-written, but will be created if they don't exist.\n"/>

    <TextView
        android:id="@+id/install_sys"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="left"
        android:textColor="#ff0000"
        android:textSize="14dip"
        android:text=""/>

    <CheckBox android:id="@+id/install_overwrite"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:paddingBottom="5dip"
              android:text=" Overwrite ALL"/>>

    <LinearLayout   android:orientation="horizontal"
                    android:gravity="center"
                    android:paddingTop="10dip"
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content">

        <Button android:id="@+id/install_changelog"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Change Log"/>

        <Button android:id="@+id/install_start"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Install System"/>

    </LinearLayout>

</LinearLayout>

</ScrollView>